class Solution {
    public int numberOfEmployeesWhoMetTarget(int[] hours, int target) {
        int count = 0;
        for (int val : hours) {
            count = (val >= target) ? count + 1 : count;
        }
        return count;
    }
}
